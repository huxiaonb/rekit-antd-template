import React, { Component } from 'react';

export default class Settings extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="kit-settings">
        Component content: kit/Settings
      </div>
    );
  }
}
